sudo: required

services:
  - docker

language: python

python:
  - 3.6

before_script:
  - docker build -t my-test-qt .
  - docker run my-test-qt

script:
  - python3 -m unittest discover -s test -p "*_test.py"
  - mysql -h localhost -u travis <<<'SELECT 2+3;'